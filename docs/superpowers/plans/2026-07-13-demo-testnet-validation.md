# Demo/Testnet Safety Remediation and Validation Record

> **Execution rule:** credentialed writes are non-production only, globally serial, bounded by the configured notional envelope, and must not mutate pre-existing account state. Credentials, private keys, account addresses, proxy details, signatures, and signed payloads are never recorded in this artifact.

**Goal:** make every configured Demo/Testnet target fail closed and obtain real read, adapter, runtime, private-stream, fill, close, and cleanup evidence where each venue's approved scope permits it.

**Environment:** all nine venue credential key names are present in `.env`. Live commands source the file without echoing values. The latest serialized validation run used the configured proxy first and a direct-path retry only for read/reference probes. Binance, OKX, Bybit, and Bitget timed out before a usable connection on both paths; this is recorded as an external blocker rather than converted to a skip. Credentialed writes were never attempted for a venue whose read/reference prerequisite failed.

## Finding checklist

Findings are grouped by severity. Every repair preserves existing public APIs unless an additive API was required.

| ID | Finding | Severity | Affected files | Planned/final repair | Regression and live verification | Status |
| --- | --- | --- | --- | --- | --- | --- |
| DV-21 | Repository `.env` could activate live reads/writes, realtime WebSockets, or unsafe custom-write gates during an ordinary test process | CRITICAL | `internal/testenv/testenv.go`, `internal/testenv/testenv_test.go` | Never import any `RUN_*`, `*_REALTIME_WS`, or variable whose key starts with or contains `ENABLE_`/`ALLOW_` from `.env`; evaluate process-local gates before loading repository credentials/config | `TestLoadRepoEnvDoesNotImportExecutionGates` and `TestRequireLiveWriteDoesNotImportEnableFlagFromRepoEnv`; targeted normal/race tests and full gates | **FIXED; FAIL-CLOSED** |
| DV-22 | Repository credential files were not protected by a root ignore contract | HIGH | `.gitignore`, `internal/testenv/makefile_contract_test.go` | Ignore `.env` and `.env.*` while explicitly allowing `.env.example` | `TestRepositoryGitignoreProtectsEnvironmentCredentials`; `git check-ignore` | **FIXED** |
| DV-01 | Shared Perp acceptance could flatten pre-existing exposure and emergency cleanup was unbounded | HIGH | `adapter/internal/runtimeaccept/order_lifecycle.go`, venue acceptance builders/tests | Reject every non-zero pre-existing position report; retain the compatibility field but disable destructive semantics; cap cleanup to observed lifecycle-created exposure | Dirty/hedged/offsetting-position and cleanup-bound regressions; serial venue lifecycles | **FIXED; live matrices passed where run** |
| DV-02 | Binance/OKX aggregates could pass after `t.Skip` | HIGH | `Makefile`, `internal/testenv/makefile_contract_test.go` | Use exact `noskipgotest` leaves and make write aggregates non-parallel | Makefile contract test; historical complete Binance/OKX aggregates emitted RUN/PASS and no SKIP | **FIXED; fresh exchange rerun blocked at read connectivity** |
| DV-03 | Bitget endpoint overrides could lose PAP semantics or route signed writes outside Demo | HIGH | `internal/testenv/testenv.go`, `sdk/bitget/*`, adapter acceptance/tests | Keep `paptrading` independent of endpoints; accept official PAP by default; require explicit TLS custom-write opt-in; reject known production WS hosts | Config/header/profile regressions; historical complete Bitget aggregate | **FIXED; fresh exchange rerun blocked at read connectivity** |
| DV-09 | Hyperliquid acceptance lacked bounded fills, Spot cleanup, Perp/HIP-3 close-to-flat, and adapter HIP-3 coverage | HIGH | `adapter/hyperliquid/{spot,perp}/*acceptance*`, `Makefile`, shared lifecycle helpers | Add bounded fill/close flows, account-backed cleanup, strict HIP-3 selectors, adapter and runtime rows, private-stream evidence, and exact final-state checks | Offline selector/account/lifecycle tests; fresh standard Spot/Perp read+adapter+runtime rows passed; HIP-3 read failed twice on an empty book and prevented HIP-3 writes | **FIXED; FRESH LIVE PARTIAL (external liquidity blocker)** |
| DV-10 | Demo writes could activate from credentials alone and enabled failures could become successful skips | HIGH | `internal/testenv/testenv.go`, venue acceptance tests, `Makefile` | Require explicit command-local write flags; missing flags remain offline-safe; activated leaves fail on dirty state, funding, credentials, or network | Plain full suites perform no writes; Make contract tests; live matrices | **FIXED** |
| DV-11 | Binance/OKX cleanup could arm before a proven fill, use account-wide cancellation, or issue duplicate sells | HIGH | Binance/OKX acceptance helpers/tests | Track only acceptance-created IDs; arm exposure cleanup only after authoritative fills; cap by observed quantity; never blindly retry ambiguous close | Unrelated-order, ambiguous-fill, delayed-balance, hedged/over-limit and original-error regressions; historical complete live aggregates | **FIXED; fresh exchange rerun blocked at read connectivity** |
| DV-12 | Binance Perp netting allowed equal long/short legs to pass flat preflight | HIGH | Binance Perp acceptance helpers/tests | Require every individual position report to be zero before submit and at final state | Offset `+q/-q` adapter/runtime regressions; historical complete Binance aggregate | **FIXED; fresh exchange rerun blocked at read connectivity** |
| DV-13 | OKX credentialed writes allowed unsafe custom/plaintext/production profiles | HIGH | `internal/testenv/testenv.go`, tests, OKX fixtures/docs | Restrict official writes to simulated-trading TLS profiles; custom writes require explicit opt-in and TLS; reject production routes | Profile matrix regressions; historical complete OKX Demo aggregate | **FIXED; fresh exchange rerun blocked at read connectivity** |
| DV-15 | Nado acceptance had an outer account-wide `CancelAll` defer | HIGH | `adapter/nado/testnet_acceptance_test.go`, unit tests | Remove account-wide cleanup; clean only recorded lifecycle order IDs | Source safety regression; complete Nado aggregate at cap `110 USDT0` | **FIXED; LIVE PASS** |
| DV-16 | Bitget custom credentialed endpoints could disclose Demo credentials/signatures | HIGH | `internal/testenv/testenv.go`, SDK redaction tests, docs | Require PAP plus HTTPS/WSS and explicit custom-write opt-in; sanitize diagnostics | Endpoint/redaction regressions; historical official-PAP live aggregate | **FIXED; fresh exchange rerun blocked at read connectivity** |
| DV-18 | Lighter book aggregation accepted malformed or non-positive price/size levels | HIGH | `adapter/lighter/market.go`, `adapter/lighter/market_test.go` | Strictly parse price and size, reject non-positive levels, and canonicalize price keys before aggregation | `TestAggregateLighterBookLevelsDropsMalformedAndNonPositivePrices`; complete Lighter aggregate | **FIXED; LIVE PASS** |
| DV-20 | Full Spot fills recomputed proportional close quantity one tick low, causing Aster min-notional cleanup failure | HIGH | `adapter/internal/runtimeaccept/order_lifecycle.go`, lifecycle tests | For a complete fill, use the exact configured guarded close quantity; retain proportional rounding only for partial fills | `TestGuardedSpotFullFillKeepsConfiguredCloseQuantityAtTickBoundary`; historical complete Aster aggregate and fresh Spot adapter/runtime passes | **FIXED; latest Perp runtime has a separate account-state blocker** |
| DV-04 | Hyperliquid/Lighter rounding could exceed configured max notional | MEDIUM | venue acceptance selectors/callsites/tests | Validate positive inputs, step/minimum constraints, and final quantity×price cap after rounding | Impossible-step/minimum and aligned-quantity regressions; live notional evidence | **FIXED** |
| DV-05 | Live risk probes could submit disaster-sized requests if wiring regressed | MEDIUM | `adapter/internal/runtimeaccept/account_state.go`, runtime acceptance tests | Prove venue-handoff ordering offline with fakes; live probes use cache-backed local risk checks only | Zero-journal/cache/inflight/venue fake regression; source review and live runs | **FIXED** |
| DV-06 | Spot lifecycle lacked safe asset recovery after an ambiguous/failed close | MEDIUM | shared lifecycle helper and venue acceptance tests | Install baseline guards; recover only authoritative observed test deltas; refuse blind duplicate sells | Partial/ambiguous close, baseline-inventory, fee-reserve and dust regressions; final balance checks | **FIXED; historical failed-run residuals reported below** |
| DV-07 | Write aggregates permitted `make -j` account-state races | MEDIUM | `Makefile`, contract test | Add every credentialed aggregate to `.NOTPARALLEL`; execute venues globally serial in this run | Makefile contract regression; serialized command record | **FIXED** |
| DV-14 | Binance market buys and OKX crossed IOC prices could exceed the cap; runtime omitted the cap | MEDIUM | Binance/OKX builders, runtime risk setup/tests | Use bounded IOC prices, size against the highest lifecycle price, and install runtime max-notional before handoff | Crossed-price boundary and zero-handoff regressions; historical live aggregates | **FIXED; fresh exchange rerun blocked at read connectivity** |
| DV-17 | Bybit's fixed 5000 ms REST window was too short for the observed Demo proxy path | MEDIUM | `sdk/bybit/{client,auth}.go`, auth tests, `adapter/bybit/{adapter,demo_acceptance,foundation}*` | Keep the SDK default at 5000 ms; add `WithRecvWindowMillis`; use 15000 ms only in the internal Demo acceptance construction and preflight | GET/POST header-and-signature tests; adapter propagation test; SDK/adapter suites pass. Live rerun is blocked by TCP timeout after the original `10002` failure | **FIXED OFFLINE; LIVE RETRY BLOCKED** |
| DV-19 | Lighter one-tick best bid became a zero resting price after 0.99× flooring | MEDIUM | `adapter/lighter/testnet_acceptance_test.go`, unit test | Preserve a positive one-tick bid while still requiring a resting price below the ask | `TestLighterRestingPriceKeepsOneTickBestBidPositive`; complete Lighter aggregate | **FIXED; LIVE PASS** |
| DV-23 | A legacy alias could override an explicitly empty canonical variable | MEDIUM | `internal/testenv/testenv.go`, `internal/testenv/testenv_test.go` | Respect canonical presence even when its value is empty; import legacy values only when canonical is unset | `TestLoadRepoEnvLegacyAliasDoesNotOverrideExplicitEmptyCanonical` | **FIXED** |
| DV-24 | Sanitized config rendering did not remove every URL credential carrier | MEDIUM | `internal/testenv/testenv.go`, `internal/testenv/testenv_test.go` | Remove URL userinfo, query, and fragments from config output and validation errors; redact malformed values wholesale | Config String/GoString and validation-error redaction regressions under normal/race/vet | **FIXED** |
| DV-08 | Unknown Gate overrides were mislabeled official Testnet | LOW | `internal/testenv/testenv.go`, tests | Mark only the known official Testnet host set as write-safe; custom hosts remain read-diagnostic only | Unknown-host write failure regression; historical complete Gate Testnet aggregate; fresh reference/read/Spot adapter passed before runtime transport timeout | **FIXED; FRESH LIVE PARTIAL** |

## Validation matrix

All live write commands were run one venue at a time. Successful leaves emitted explicit RUN/PASS evidence and no SKIP. The table separates the latest serialized validation run from earlier live evidence so a historical pass is not mistaken for current connectivity.

| Venue | Latest serialized read/reference | Latest serialized adapter/runtime writes | Earlier evidence and current safety conclusion |
| --- | --- | --- | --- |
| Binance Demo | Reference probe timed out through both configured proxy and direct paths | Not run because the read prerequisite failed | An earlier complete aggregate passed; final reconnect changes have full offline normal/race coverage but no fresh exchange-backed evidence |
| OKX Demo | Reference probe timed out through both configured proxy and direct paths | Not run because the read prerequisite failed | An earlier complete aggregate passed; final transaction/replay changes have offline normal/race coverage but no fresh exchange-backed evidence |
| Bybit Demo | Reference probe timed out through both configured proxy and direct paths | Not run because the read prerequisite failed | Earlier Spot adapter/runtime and USDT Perp adapter rows passed; the remaining rows are blocked after the scoped receive-window repair, and the blocked retries submitted nothing |
| Bitget Demo | Reference probe timed out through both configured proxy and direct paths | Not run because the read prerequisite failed | An earlier complete official-PAP aggregate passed; final reconnect/debug-redaction changes have offline normal/race coverage but no fresh exchange-backed evidence |
| Gate Testnet | Reference and Spot read passed | Spot adapter passed; Spot runtime then timed out, so all later rows were stopped | An earlier complete aggregate passed. The fresh timeout stage does not prove cleanup/final snapshot completion, so current open-order/base-balance state remains unproven rather than assumed clean |
| Hyperliquid Testnet | Reference plus standard Spot/Perp reads passed; HIP-3 read failed twice because the configured market had an empty two-sided book | Standard Spot and Perp adapter/runtime rows all passed; HIP-3 writes were not run | Standard-market cleanup returned to the guarded baseline. HIP-3 remains an external liquidity blocker; historical Spot dust/residual is noted below |
| Lighter Testnet | Not repeated in the final connectivity pass | Not repeated in the final connectivity pass | Earlier complete five-leaf aggregate passed; approved scope is resting submit/cancel rather than fill/close |
| Aster Testnet | Reference plus Spot/Perp reads passed | Spot adapter/runtime and Perp adapter passed; Perp runtime failed twice on a cleanup/account-state condition | An earlier post-repair complete aggregate passed, but the latest account state is not proven clean; no speculative cleanup was issued |
| Nado Testnet | Spot/Perp/reference reads all passed | Spot/Perp adapter and runtime rows all passed with `NADO_TESTNET_MAX_NOTIONAL_USDT0=110` | Fresh complete aggregate evidence; no failure or skip was observed |

## Required repository gates

- [x] `gofmt` on all changed and untracked Go files.
- [x] `go test ./... -count=1` with every live-write/read flag explicitly disabled.
- [x] `go test -race ./... -count=1` with every live-write/read flag explicitly disabled.
- [x] `go vet ./...` with every live-write/read flag explicitly disabled.
- [x] `git diff --check`.
- [x] `staticcheck ./...` availability checked; binary is not installed.
- [x] Finish the final independent read-only reviews and repair any valid finding.
- [ ] Retry fresh Binance/OKX/Bybit/Bitget references and their gated aggregates if connectivity returns.

## Residual external state and safety boundary

- An early Aster Spot run, before DV-20 was fixed, left a measured test-created delta of `8.0823145` base units. The safe planned close from that lifecycle is `8.04`, leaving the intended fee buffer. No blind cleanup is allowed unless current free/total balances, the original baseline, current minimum notional, and absence of unrelated open orders are all re-proven.
- The latest Aster Perp runtime row failed twice on a cleanup/account-state condition after its read and adapter prerequisites passed. The exact current position/order state was not proven from a separate authoritative snapshot, so no compensating write was guessed. This remains a live-state blocker until a read-only proof can distinguish a pre-existing condition from lifecycle-created residual state.
- The fresh Gate Spot runtime row ended with a transport timeout after earlier Gate prerequisites had passed. The sanitized result does not prove whether submit occurred or whether deferred exact cleanup and its final snapshot completed. No residual is asserted, but current open orders and base balance remain an evidence blocker until an independent authoritative read-only snapshot succeeds.
- An early Hyperliquid Spot run left approximately `5.9958` PURR. The instrument size step permits at most the original planned close of `5`, leaving sub-step dust. Cleanup must use a unique IOC ClientID and exact status/fill reconciliation; it must not call account-wide cancellation or increase quantity to satisfy minimum notional.
- Neither residual is silently sold from a possibly shared account. If the baseline proof is incomplete or connectivity prevents exact reconciliation, the residual remains an explicitly reported blocker.

## Coverage boundary

Lighter's approved Testnet scope proves real resting submit/cancel plus runtime reconciliation, not fill/close. Hyperliquid's approved scope is stronger: the fresh run proves bounded standard Spot/Perp adapter and runtime lifecycles, while HIP-3 could not pass its read prerequisite because the configured live book was empty and therefore remained unwritten. Binance, OKX, Bybit, and Bitget are currently blocked by connectivity before their write gates; Aster Perp is blocked by unresolved account/cleanup state rather than a safe-to-retry code path.
