# Code Review Remediation Implementation Plan

> **Historical / superseded:** This record predates the 2026-07-15
> runtime/adapter convergence PRD. Its additive-compatibility and optional risk
> lease decisions are not current requirements and must not be used to restore
> venue-specific runtime choreography. The 2026-07-15 PRD and test specification
> are authoritative; only their G008 frozen-candidate Demo/Testnet run can
> certify the converged tree.

> **For agentic workers:** REQUIRED SUB-SKILL: Use test-driven development for every finding. Ralph coordinates independent severity-matched lanes and the root agent performs integration plus final review. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix every valid finding from the `cc2524a` full-repository review without breaking existing public APIs, and prove each repair with a regression test plus full Go verification.

**Architecture:** Keep the existing ports-and-adapters boundary. Repairs add internal/additive seams for shared fill application, multiplier lookup, reconciliation verdicts, reconnect-gap notification, bounded durability, and retention; existing `core/contract` method signatures remain unchanged. Safety behavior becomes fail-closed where evidence is incomplete, while compatibility escape hatches must be explicit and additive.

**Tech Stack:** Go, standard `testing`, `shopspring/decimal`, existing runtime fake clients and local WebSocket test servers; no new dependencies.

---

## Finding checklist

| ID | Review finding | Severity | Affected files | Planned repair | Regression verification | Final status |
| --- | --- | --- | --- | --- | --- | --- |
| CR-01 | Recovered and synchronous fills do not reach Portfolio/PnL/callback semantics | HIGH | `runtime/reconcile/*`, `runtime/node.go`, `runtime/orderstate/orderstate.go`, `runtime/journal/*`, fill-capable adapters, related tests | Request cursor-bounded fill history with overlap; route recovered fills through the node apply seam; persist and validate cursor dependencies; normalize/sort fill identities; coordinate cumulative order snapshots; emit deterministic inferred fills only where no authoritative stream/history exists | Assert Portfolio/PnL/callback application, deterministic order, cumulative snapshot coverage, late-visible fills, crash-before-cursor restart dedupe, dependency validation, observer startup ordering, and synchronous fills | **FIXED** |
| CR-02 | Contract multipliers are omitted from risk, PnL, exposure, and OKX metadata | HIGH | `runtime/risk/risk.go`, `runtime/portfolio/portfolio.go`, `runtime/node.go`, `adapter/okx/perp/instrument.go`, Gate/OKX tests | Bind the market or explicit `InstrumentProvider` into Portfolio/risk, retain lot multiplier, use multiplier-aware PnL/exposure/notional math, and preserve validated OKX `CtVal` | Gate-style `0.0001` tests assert true notional `10` and PnL `0.1`; OKX conversion asserts `ContractMultiplier == CtVal`; node provider-wiring tests cover implicit and explicit providers | **FIXED** |
| CR-03 | Aggregate exposure and spot funds are not reserved; market notional limits can be skipped | HIGH | `runtime/risk/risk.go`, `runtime/exec/engine.go`, `runtime/cache/cache.go`, tests | Compute worst-case position across working orders; aggregate spot reservations by consumed asset across instruments; hold a short-lived submission reservation across Check→PendingNew; avoid double-counting orders reflected in balance snapshots; reject missing reference prices | Tests cover combined exposure, cross-instrument quote/base reservations, concurrent submissions under `-race`, free/locked snapshot cutoff, zero-price notional rejection, and in-flight ClientID retention | **FIXED** |
| CR-04 | Mismatched request AccountID can create an orphan live order | HIGH | `runtime/exec/engine.go`, `runtime/exec/engine_test.go` | Reject every non-empty request AccountID different from the engine account before validation, risk, journal, cache, or venue calls | Assert no adapter call, journal record, cache record, or in-flight mutation and a clear account mismatch error | **FIXED** |
| CR-05 | Reconciliation verdict is ignored and Resync/Reconnect clears Halt/ReduceOnly | HIGH | `runtime/reconcile/reconcile.go`, `runtime/node.go`, lifecycle/reconciliation tests | Derive an explicit activation verdict from fill/open-order completeness and blocking findings; preserve the strongest prior Halt/ReduceOnly restriction across Resync/Reconnect | Tests cover unsafe evidence staying restricted, safe open-orders-only partial reports, successful activation, and sticky Halt/ReduceOnly across Resync/Reconnect | **FIXED** |
| CR-06 | SDK automatic reconnect gaps are invisible to runtime | HIGH | `core/contract/events.go`, `runtime/node.go`, `adapter/internal/streamgap/*`, affected adapters/SDK WebSocket clients and tests | Add an additive generation-tagged stream-gap event; emit started/recovered around exact-connection auth/subscription restoration; restrict trading immediately and reconcile once before state restoration | Runtime generation tests and adapter/SDK lifecycle tests cover stale generations, exact replacement sockets, resubscription, and restoration only after reconciliation | **FIXED** |
| CR-07 | Venue acknowledgements can be lost to caller cancellation; local failures can be marked sent | HIGH | `runtime/exec/engine.go`, `adapter/{bybit,bitget,gate}/*`, tests | Persist post-boundary CommandResult using a bounded detached context; validate deterministic local conversion/instrument failures before REST; reserve ambiguous classification for possible venue handoff | Cancellation-race tests assert confirmed results are journaled/applied; adapter tests assert invalid local requests never invoke REST and remain local failures | **FIXED** |
| CR-08 | `wsstream.Close` neither closes Events nor guarantees Emit=false after close | MEDIUM | `internal/wsstream/wsstream.go`, `internal/wsstream/wsstream_test.go` | Register emitters under a mutex, signal close, wait for active emitters, close Events exactly once, and reject new/post-close emits | Normal and race tests cover blocked emit unblocking, receive-channel closure, concurrent Close, and repeated post-close Emit=false | **FIXED** |
| CR-09 | Failed Bitget/Bybit private-WebSocket authentication leaves a false-connected socket | MEDIUM | `sdk/bitget/private_ws.go`, `sdk/bybit/private_ws.go`, lifecycle tests | Track authenticated state separately; on reject/timeout/cancel clear and close the exact socket; reconnect only after a previously authenticated connection drops | Local WebSocket tests force reject/timeout and prove retry uses a fresh dial/auth; race suite verifies connection-state synchronization | **FIXED** |
| CR-10 | Runtime memory/replay structures have no explicit retention bounds | MEDIUM | `runtime/exec/fill_buffer.go`, `runtime/reconcile/reconcile.go`, `runtime/risk/risk.go`, `runtime/cache/cache.go`, `runtime/journal/journal.go`, retention tests | Add configurable test limits and conservative defaults; evict only completed/inactive history; retain open intents, active/Unknown orders, unresolved findings, pending durability work, and latest valid cursors | Small-limit and reopen tests assert bounded in-memory state while active recovery records survive; full race suite covers concurrent retention paths | **FIXED** |

## Execution order

### Task 1: HIGH — Fill and reconciliation closure (CR-01)

**Files:** `runtime/reconcile/*`, `runtime/node.go`, `runtime/journal/*`, Hyperliquid/Lighter execution code and tests.

- [x] Add focused failing tests for recovered fills, durable applied-event ordering, and synchronous filled responses.
- [x] Run only those tests and record expected failures caused by missing Portfolio/callback/journal behavior.
- [x] Implement the smallest shared apply seam and bounded fill query/cursor behavior.
- [x] Run focused package tests until green, then run `go test -short ./runtime/... ./adapter/hyperliquid/spot/... ./adapter/lighter/...`.

### Task 2: HIGH — Multiplier and aggregate risk correctness (CR-02, CR-03)

**Files:** `runtime/portfolio/*`, `runtime/risk/*`, `runtime/cache/*`, `runtime/node.go`, Gate/OKX instrument conversion tests.

- [x] Add failing tests for multiplier-aware PnL/notional/exposure, pending-order worst-case position, pending spot reservations, and zero-reference market notional.
- [x] Run focused tests and confirm each fails for the reviewed arithmetic/omission.
- [x] Implement provider-backed multiplier math and conservative reservation checks without changing existing method signatures.
- [x] Run `go test -short ./runtime/risk/... ./runtime/portfolio/... ./runtime/cache/... ./adapter/gate/... ./adapter/okx/perp/...`.

### Task 3: HIGH — Account and command-boundary integrity (CR-04, CR-07)

**Files:** `runtime/exec/*`, Bybit/Bitget/Gate execution clients and tests.

- [x] Add failing tests for mismatched AccountID, acknowledgement/cancellation race, and local adapter validation failures.
- [x] Run focused tests and confirm the adapter/journal/cache symptoms.
- [x] Add early account equality validation, detached result persistence, and missing adapter preflight validators.
- [x] Run `go test -short ./runtime/exec/... ./adapter/bybit/... ./adapter/bitget/... ./adapter/gate/...`.

### Task 4: HIGH — Lifecycle and reconnect safety (CR-05, CR-06)

**Files:** `core/contract/events.go`, `runtime/node.go`, `adapter/internal/streamgap/*`, affected adapter/SDK WebSocket wiring and tests.

- [x] Add failing tests for sticky Halt/ReduceOnly, unsafe reconciliation verdicts, and automatic reconnect generation handling.
- [x] Run focused tests and confirm each currently reactivates or fails to notify.
- [x] Implement additive verdict and notification seams, preserving existing contract methods.
- [x] Run `go test -short ./core/... ./runtime/... ./adapter/binance/... ./sdk/binance/...` and the runtime race suite.

### Task 5: MEDIUM — Stream shutdown and private authentication (CR-08, CR-09)

**Files:** `internal/wsstream/*`, Bitget/Bybit private-WebSocket clients and tests.

- [x] Add failing concurrent-close and failed-auth retry tests.
- [x] Run them and confirm the contract violations.
- [x] Implement coordinated channel closure and authenticated connection state.
- [x] Run focused tests under normal and race modes.

### Task 6: MEDIUM — Bounded retention (CR-10)

**Files:** runtime fill/reconcile/risk/cache/journal state and tests.

- [x] Add small-capacity failing tests for every retained collection.
- [x] Confirm growth exceeds the configured test limits before implementation.
- [x] Add bounded FIFO/window policies that never evict active recovery state.
- [x] Run focused persistence/reopen/race tests.

### Task 7: Integration, review, and verification

- [x] Run `gofmt` on every changed Go file.
- [x] Run fresh `go test ./... -count=1` after the final Bybit receive-window repair.
- [x] Run fresh `go test -race ./... -count=1` after the final Bybit receive-window repair.
- [x] Run fresh `go vet ./...`.
- [x] Check `staticcheck ./...` availability (not installed in this environment).
- [x] Inspect `git diff --check`, `git diff --stat`, and the complete final diff.
- [x] Complete the final independent runtime, SDK, adapter/live-safety, and architect review; repair every valid issue and repeat affected verification.
- [x] Repeat changed-files-only ai-slop review after the last accepted finding.
- [x] Finalize every checklist row with accepted findings, rejected false positives, live evidence, and blockers.

## Plan self-review

- Every accepted original finding CR-01 through CR-10 and every final-review RR/DV addendum maps to files, severity, repair, and a verification method; bug fixes have direct regressions.
- Findings were repaired in severity order within each audit round; newly discovered CRITICAL/HIGH issues immediately preempted lower-priority cleanup before final verification.
- No existing public method was removed. New surfaces are additive, and `WithInFlightResolver` only relaxed its required callback capability so all prior call arguments remain source-compatible while newer identity methods are detected optionally.
- Credentialed exchange I/O is restricted to globally serial Demo/Testnet acceptance and is recorded separately in `2026-07-13-demo-testnet-validation.md`.
- No placeholder task remains; any implementation adjustment must preserve the observable behavior and test named in its checklist row.

## Final-review addenda

Independent runtime and architect reviewers found the following additional issues while auditing the implementation. Each was reproduced before repair and re-reviewed after its regression turned green.

| ID | Finding | Severity | Affected files | Final repair | Verification |
| --- | --- | --- | --- | --- | --- |
| RR-01 | A reconciliation cursor could be accepted without every referenced applied-event record | CRITICAL | `runtime/journal/journal.go`, `runtime/reconcile/state.go`, journal/reconcile tests | Validate cursor dependencies on append and replay; ignore invalid replay cursors with an explicit warning; retain committed dependency state across compaction | `runtime/journal/dependency_regression_test.go`; normal/race full suites |
| RR-55 | A finding that legitimately reopened during its resolution remained durable in `JournalStateStore`, but the current reconciliation report unconditionally removed the logical ID and could return an activation-safe verdict | CRITICAL | `runtime/reconcile/reconcile.go`, `runtime/reconcile/finding_resolution_regression_test.go` | After durable resolution, remove the captured version and reload the authoritative open set for the pass scope; merge it back by logical ID so a concurrent/reentrant reopen remains visible and blocking, and fail closed if the refresh cannot be loaded | `TestReconcilerResolutionReportRetainsReentrantFindingReopen`, prior store-level reentrant reopen tests, position/order resolution regressions under normal/race |
| RR-02 | Cursor windows could skip fills after the requested `Until` or fills that became visible late with a slightly older venue timestamp; pending applied events could be stranded | HIGH | `runtime/reconcile/reconcile.go`, `runtime/reconcile/state.go`, `runtime/reconcile/fill_durability_regression_test.go` | Advance only to the requested coverage boundary, retain a one-minute bounded overlap, flush pending durability before every pass, block cursor commits on unresolved fills, and seed retained uncommitted applied events after a crash | `TestCursorWatermarkDoesNotExceedRequestedFillWindow`, `TestCursorOverlapRecoversLateVisibleVenueTimestamp`, `TestPendingAppliedFillFlushesBeforeCursorAdvance`, restart tests |
| RR-03 | Fill identity enrichment, cumulative order snapshots, and map iteration could duplicate or reorder recovered fills | HIGH | `runtime/orderstate/orderstate.go`, `runtime/cache/cache.go`, `runtime/reconcile/reconcile.go`, runtime/reconcile tests | Canonicalize fill/order identity, sort reports deterministically, retain snapshot baseline coverage, reapply only uncovered fill quantity, and use the merged canonical cache order | Cumulative snapshot, canonical identity, identity enrichment, venue-time ordering, and synthetic-ID regression tests |
| RR-04 | Concurrent submissions and cross-instrument Spot orders could bypass aggregate exposure/cash limits or double-count balance-locked funds | HIGH | `runtime/risk/risk.go`, `runtime/exec/engine.go`, `runtime/node.go`, risk/exec tests | Add an optional atomic submission-risk lease, aggregate Spot reservations by consumed asset, protect in-flight ClientIDs, bind instrument providers, and compare working-order time against balance snapshot time | Concurrent submission tests repeated under `-race`; cross-instrument quote/base and free/locked cutoff tests |
| RR-05 | Requiring journal record enumeration on every pass broke valid custom `journal.Store` implementations, while silently skipping enumeration would make recovered fills crash-unsafe | HIGH | `runtime/reconcile/state.go`, `runtime/reconcile/reconcile.go`, `runtime/reconcile/fill_durability_regression_test.go` | Capability-gate retained-record replay: allow empty passes on opaque stores, but fail closed before any cache/Portfolio/callback mutation when a recovered fill requires unavailable crash replay | `TestOpaqueJournalAllowsReconciliationWithoutRecoveredFills`, `TestOpaqueJournalRejectsRecoveredFillBeforeBusinessApplication`, direct replay fail-closed test; independent reviewer PASS |
| RR-06 | Bounded fill indexes could forget the immediately preceding report after a partial/error/cursor-commit failure; a partial cursor also omitted earlier durable dependencies and allowed compaction to reapply fills after restart | HIGH | `runtime/reconcile/reconcile.go`, `runtime/reconcile/overlap_retention_regression_test.go`, `runtime/reconcile/overlap_outcome_regression_test.go` | Add an independently bounded exact overlap set; replace it only after a complete committed pass, merge it on every incomplete outcome, fail closed before applying an identity that cannot be retained, and union all retained record IDs into partial cursors | Full→partial→full, mid-report failure, empty cursor-commit failure, capacity exhaustion, no-store three-pass, and Journal retention=1 full A-E→partial D/E→restart regressions; independent runtime reviewer PASS |
| RR-07 | Aster Spot/Perp WebSocket writes could block `Close`, outlive their connection, or publish a recovered generation before exact subscription restoration | HIGH | `sdk/aster/{spot,perp}/ws_client.go`, `ws_write_lifecycle_test.go`, `ws_v3_lifecycle_test.go` | Bind every read/write/heartbeat to the exact connection epoch, bound writes with deadlines, detach/close without waiting behind stale I/O, restore subscriptions transactionally, and serialize generation callbacks | Blocked-write interruption/deadline/replacement tests plus reconnect callback-order tests under normal/race; package vet PASS |
| RR-08 | Binance private streams could clobber replacement sockets, drop or reorder recovery generations, reuse a stale listen key forever, or mark a stale Spot subscription recovered | HIGH | `sdk/binance/{spot,perp}/ws_api.go`, `ws_account.go`, lifecycle/recovery tests | Capture exact connections, coalesce rather than drop recovery triggers, acquire a fresh Perp listen key immediately, bind Spot subscription state to account and transport epochs, serialize lifecycle callbacks, and suppress stale reconnect completions | Stale-reader, all-stale-handshake, trigger-during-recovery, callback-order, disconnect-before-completion, manual-replacement, close-during-backoff, and concurrent-Close regressions; Spot security reviewer PASS |
| RR-09 | Signed/private request diagnostics could expose payloads, signatures, proxy credentials, or query-bearing raw endpoints | HIGH | Binance/Aster and other affected SDK logging/error constructors; redaction tests | Log only event/request type and byte counts, strip URL query/userinfo from errors, and redact proxy/signature material at every diagnostic boundary | Transport/status sentinels and signature/private-payload/proxy log-observer tests across affected SDK packages |
| RR-10 | Reconciliation findings used unstable occurrence-derived IDs, so the same blocking condition could accumulate and defeat bounded retention | MEDIUM | `runtime/reconcile/reconcile.go`, `runtime/reconcile/finding_retention_regression_test.go` | Derive finding identity from stable scope/stream/code/severity/blocking fields while retaining occurrence timestamps as data | Repeated identical finding remains one retained logical record; distinct findings remain distinct |
| RR-11 | Binance Spot/Perp account event handlers invoked user callbacks while holding the client mutex, deadlocking reentrant callback registration or lifecycle calls | MEDIUM | `sdk/binance/{spot,perp}/ws_account.go`, `ws_account_callback_lock_test.go` | Snapshot callback slices under lock and invoke the snapshots after unlocking | Every Spot and Perp account-event class asserts the callback runs without the client mutex; repeated normal/race package tests |
| RR-12 | Binance Perp transport heartbeats could survive reconnect and write the replacement connection; account `Close` also accidentally changed the existing public Close→Connect restart behavior | MEDIUM | `sdk/binance/perp/ws_client.go`, `ws_account.go`, lifecycle/restart regression tests | Bind heartbeat goroutines to their exact socket and use restartable account-lifecycle generations so stale recovery cannot mutate a newly explicit connection | Exact-socket heartbeat regression plus Close→Connect fresh-listen-key/stale-callback regression; package normal/race/vet |
| RR-13 | Nado could replace an authoritative subscription rejection with a generic connection-generation error when the peer closed immediately after its acknowledgement | MEDIUM | `sdk/nado/ws_account.go`, `sdk/nado/ws_lifecycle_test.go` | Treat a successful exact-socket write as the handoff boundary and let the request-specific waiter decide acknowledgement versus connection-loss precedence; keep exact-generation checks before the write and on every later replay item | Deterministic rejection-then-close barrier is old-code RED and preserves `invalid wildcard stream`; exact stale replay still rejects before a second write; focused normal/race and full race PASS |

### Adapter and SDK audit addenda

The second independent audit expanded beyond the original CR/RR set. These findings were handled in severity order and each has a direct regression.

| ID | Finding | Severity | Affected files | Final repair | Verification |
| --- | --- | --- | --- | --- | --- |
| A-01 | Bybit, Bitget, and Gate mass status omitted authoritative fill history or scoped it inconsistently | HIGH | `adapter/{bybit,bitget,gate}/execution.go`, account/convert helpers, `mass_status_fill_test.go` | Query, normalize, paginate, deduplicate, and scope fill reports consistently with open-order/position evidence | Venue-specific mass-status fill, cursor, pagination, duplicate, and scope regressions; adapter package suites |
| A-02 | Gate wire conversion and position/account-mode semantics could misclassify orders or state | HIGH | `sdk/gate/{types,private_rest}.go`, `adapter/gate/{account,convert,events,execution,position_mode}.go` | Model official wire fields, normalize one-way/dual position modes, validate local requests, and preserve account/settlement scope | `position_mode_test.go`, execution validation, contract, private-stream and mass-status regressions; Gate Testnet aggregate PASS |
| A-03 | Hyperliquid WebSocket callbacks could consume each other's messages; subscriptions were not acknowledgement-confirmed and retained public subscriptions were not replayed after manual reconnect | HIGH | `sdk/hyperliquid/{ws_client,ws_dispatch}.go`, ACK/dispatch/reconnect tests | Add one connection-owned dispatcher, request/subscription routing, confirmed subscription APIs, transactional replay, and retained-subscription replay on fresh `Connect` | Dispatcher isolation, ACK failure/timeout, reconnect gap, retained replay, stale generation, and normal/race SDK tests |
| A-04 | Hyperliquid Unified/Portfolio margin balance authority was wrong and malformed `spotState` could be silently accepted | HIGH | `adapter/hyperliquid/internal/account/state.go`, Spot/Perp `adapter.go`, `account.go`, `events.go`, `sdk/hyperliquid/spot/ws_account.go` | Use `spotState` as the authoritative balance source for Unified/Portfolio, clearinghouse only for positions; strictly validate currencies/balances, emit tombstones, surface decode gaps, and reject unknown abstraction modes | Spot/Perp account stream, malformed snapshot, omitted-currency, REST/WS tombstone race, unknown-mode, and live runtime regressions |
| A-05 | Hyperliquid order semantics and historical `userFills` snapshot provenance were conflated | HIGH | `adapter/hyperliquid/internal/ordersemantics/*`, Spot/Perp convert/execution/events, SDK order/account types | Centralize order state/quantity semantics, retain `FromSnapshot` as provenance, and deduplicate/apply real historical fills rather than discarding snapshots | Order semantics, snapshot provenance, fill identity, cancel/fill and runtime reconciliation tests |
| A-06 | Private WebSocket subscriptions and reconnect recovery were not transactionally restored across multiple venues | HIGH | Binance/Aster/Bitget/Bybit/Gate/Hyperliquid/Nado/OKX SDK clients, adapter stream-gap wiring/tests | Bind state to exact connection generations, wait for auth/subscription results, rollback partial subscriptions, serialize gap callbacks, and recover only after exact replay succeeds | Venue lifecycle/transaction/reconnect/liveness tests under normal and race modes; full race suite PASS |

### Post-integration audit addenda

The final changed-files audit found the following additional defects. They are listed in remediation order (HIGH before MEDIUM); every row was reproduced against the pre-fix implementation and has a dedicated regression.

| ID | Finding | Severity | Affected files | Final repair | Verification |
| --- | --- | --- | --- | --- | --- |
| RR-14 | A reused fill identity could silently bind to a different order, and FillBuffer could consume that identity before Cache accepted the business mutation | HIGH | `runtime/cache/cache.go`, `runtime/exec/fill_buffer.go`, `runtime/node.go`, fill identity/cache regression tests | Validate both order aliases for every repeated FillKey; perform the Cache mutation inside the FillBuffer acceptance transaction; mark the identity only after Cache success | `TestLiveTradeIDReuseAcrossOrdersHaltsWithoutMutatingSecondOrder`, `TestCacheCommitFailureDoesNotConsumeFillBufferIdentity`; repeated runtime normal/race/vet |
| RR-15 | Crossed client/venue order aliases were silently ignored in snapshots, live events, and confirmed Submit/Modify responses while reconciliation or in-flight state still advanced | HIGH | `runtime/cache/cache.go`, `runtime/node.go`, `runtime/exec/engine.go`, `runtime/reconcile/reconcile.go`, order identity regressions | Add checked, read-before-apply order upserts; fail closed before resolving in-flight work; emit a blocking reconciliation finding and withhold the cursor on snapshot conflicts | Snapshot/live/Submit/Modify crossed-alias regressions; repeated runtime normal/race/vet |
| RR-17 | Hyperliquid and Nado replay, plus OKX ready-socket/replay, could expose a replacement connection before exact subscription restoration or lose authoritative failure state | HIGH | `sdk/hyperliquid/ws_client.go`, `sdk/nado/ws_account.go`, `sdk/okx/{ws_client,ws_order,ws_callback}.go`, lifecycle/transaction tests | Make Connect+replay one exact-generation transaction, gate writes on the ready socket, retain recovery on callback enqueue failure, and make explicit OKX Connect take over auto-backoff without duplicate replay | Venue transaction, stale-generation, enqueue-failure, explicit-takeover and callback-order tests under normal/race; package vet |
| RR-18 | Replacement private-stream data could overtake the synchronous Recovered hook on Aster, Binance, Bybit, and Gate | HIGH | `sdk/aster/{spot,perp}/ws_*`, `sdk/binance/{spot,perp}/ws_*`, `sdk/bybit/private_ws*`, `sdk/gate/ws_*`, callback-order tests | Pause/buffer exact replacement-generation data, enqueue Started after already accepted old data, run Recovered synchronously, then release replacement data; discard failed/stale/closed-generation buffers | Deterministic `old -> Started -> Recovered -> replacement` regressions, failure/Close/bounded-buffer tests, package normal/race/vet and repeated key cases |
| RR-19 | Bitget opened a reconnect gap by clearing already accepted old-generation callbacks, allowing Started to overtake the old backlog | HIGH | `sdk/bitget/private_ws.go`, `sdk/bitget/private_ws_liveness_test.go` | Drain the in-flight callback and complete accepted old queue before Started; keep replacement-generation events in a separate bounded buffer until Recovered | `TestPrivateWSRecoveryDrainsQueuedOldDataBeforeLifecycle`; normal/race/vet, repeated recovery/new-gap/overflow/Close cases; independent review |
| RR-22 | Binance lifecycle and data callbacks could deadlock when reentering Close; proactive Perp recovery could let Started overtake an accepted old callback, while a stale Spot completion could unpause a newer generation | HIGH | `sdk/binance/{spot,perp}/ws_client.go`, `ws_account.go`, `internal/wsdispatch/ws.go`, dispatch/recovery regressions | Track in-flight read-loop callbacks so reentrant Close never waits on its own goroutine; use an exact-source pause/drain/drop barrier before Perp Started; revalidate lifecycle/source/epoch after the drain; keep newer Spot barriers paused when an older completion fails | Real-socket Handler/Started/Recovered/buffered-callback Close tests, manual old-data drain ordering, close-during-drain suppression, and stale-completion/newer-generation tests under normal/race repeats |
| RR-24 | Duplicate ClientIDs and late results could replace unrelated in-flight recovery state or cross the venue boundary twice | HIGH | `runtime/exec/{engine,inflight}.go`, `runtime/cache/cache.go`, execution regressions | Reserve ClientIDs before journaling/venue calls, reject cache and in-flight reuse, make replay transactional, and validate a result against its exact intent record and typed venue owner | `TestConcurrentSubmitDuplicateClientIDIsReservedBeforeJournalAndVenue`, `TestInFlightLateResultDoesNotResolveReplacementWithSameClientID`, `TestReplayOpenIntentsRejectsConflictingClientIDs`; normal/race repeats |
| RR-25 | Any order event could resolve a pending Cancel/Modify even when it did not prove that command took effect | HIGH | `runtime/exec/engine.go`, `runtime/node.go`, `runtime/reconcile/reconcile.go`, command-evidence regressions | Classify authoritative order evidence by pending command: unchanged open orders do not confirm Cancel/Modify, terminal Cancel evidence and amended terms are required, and conflicting aliases fail before resolution | `TestLiveOrderEvidenceMustMatchPendingCommand`, `TestLivePartialFillDoesNotConfirmPendingCancel`, reconciliation command-aware evidence tests |
| RR-26 | Sparse or crossed Submit/Modify responses could bind a result to the wrong account, request, ClientID, or cached venue alias | HIGH | `runtime/exec/engine.go`, `runtime/cache/cache.go`, execution identity tests | Normalize sparse acknowledgements from the original request, validate every supplied identity field, check cached alias ownership before journaling any outcome, and preserve legitimate venue-generated Modify response IDs only when unclaimed | `TestSubmitResponseClientIDMismatchFailsBeforeResultJournal`, `TestSubmitCachedVenueAliasCollisionFailsBeforeEveryResultOutcome`, `TestModifyCachedVenueAliasCollisionFailsBeforeEveryResultOutcome`, sparse-ack regression |
| RR-27 | Durable command results, cache alias changes, and in-flight clearing had separate linearization points; journal callbacks could deadlock on Cache reads and a concurrent command could use a pre-commit alias | HIGH | `runtime/cache/cache.go`, `runtime/exec/{engine,inflight}.go`, execution atomicity tests | Serialize all order writers with a dedicated order transaction lock, run journal I/O outside the global Cache lock, apply the prepared cache plan before clearing in-flight state, re-read command targets after the in-flight gate, and keep recoverability callbacks outside locks | `TestSubmitResultAndCacheAliasClaimAreAtomic`, `TestModifyConfirmedVenueAliasRotationCommitsAtomically`, `TestConcurrentCancelUsesCommittedReplacementAlias`, `TestResultJournalMayReadCacheWithoutDeadlock`, journal-failure rollback tests; normal/race repeats |
| RR-29 | Confirmed replacement aliases inherited old terminal lifecycle, could diverge between cache and returned order, and structured rejected Modify responses returned success | HIGH | `runtime/cache/cache.go`, `runtime/exec/engine.go`, execution replacement/rejection tests | Treat a changed venue alias as a new lifecycle incarnation, preserve only stronger evidence already observed for that replacement alias, return the canonical committed order, and translate Rejected/Expired responses into `ErrVenueRejected` without mutating the old order | `TestModifyVenueAliasRotationDoesNotCarryOldAliasTerminalState`, `TestModifyVenueAliasRotationReturnsCanonicalReplacementEvidence`, `TestModifyStructuredVenueRejectionReturnsErrorAndPreservesOrder` |
| RR-30 | Binance Spot advertised Modify even though its cancel-replace stream can emit old and new order incarnations with one logical ClientID, which the current runtime cannot represent safely | HIGH | `adapter/binance/spot/{capabilities,execution,contract_test,modify_identity_test}.go` | Preserve the public `Modify` method but withdraw the capability and return `contract.ErrNotSupported` before REST until multi-incarnation cache/fill/journal semantics exist | `TestBinanceSpotModifyIsDisabledBeforeREST`; Spot capability suite; adapter normal/race/vet |
| RR-31 | Confirmed Cancel and same-alias Modify could overwrite a newer canonical order event with stale request/response state | HIGH | `runtime/cache/cache.go`, `runtime/exec/engine.go`, execution atomicity regressions | Add a checked client-ID order mutation transaction, re-read the latest canonical order under the order-writer lock, preserve stronger fill/terminal/timestamp evidence, and apply only confirmed amended terms | `TestConfirmedCancelUsesLatestCanonicalOrderState`, `TestConfirmedSameAliasModifyUsesLatestCanonicalOrderState`; runtime normal/race/vet |
| RR-32 | A late adapter return could journal or apply a second definitive command result after an authoritative order event had already resolved the exact intent | HIGH | `runtime/exec/{engine,inflight}.go`, `runtime/journal/journal.go`, result-race regressions | Make exact-intent result preparation return a typed already-resolved sentinel, map the synchronous return from canonical cache state, suppress late cache mutation, and reject retained definitive-result downgrades or semantic changes | `TestModifyFirstDefinitiveResultWinsOverLateAdapterResult`, `TestCommandResultTransitionsAreMonotonic`, replay variants; normal/race/vet |
| RR-34 | Reusing a journal RecordID with a different type/payload was silently treated as idempotent, while reconciliation pass/cursor IDs did not include their full payload identity | HIGH | `runtime/journal/journal.go`, `runtime/reconcile/state.go`, conflict/identity regressions | Compare RecordIDs by type plus payload hash, retain exact result semantics in a hard-capped index that fails closed at capacity, derive reconciliation IDs from the serialized payload, and preserve known idempotent retries/conflicts even at the cap | RecordID/result replay and cross-compaction regressions, capacity/no-rewrite/semantic-retry tests, pass/cursor payload-identity regressions |
| RR-35 | The top-level capability inventory and documentation still advertised Binance Spot Modify after the adapter correctly disabled it | HIGH | `adapter/capabilities.go`, `adapter/capabilities_test.go`, `docs/adapter-capabilities.md` | Make the repository capability matrix and documentation match the fail-closed adapter contract | `TestAdapterCapabilityMatrixDoesNotClaimBinanceSpotModify`; adapter package tests |
| RR-36 | The repository had no root ignore rule for `.env` variants, leaving credential files vulnerable to accidental staging | HIGH | `.gitignore`, `internal/testenv/makefile_contract_test.go` | Ignore `.env` and `.env.*` repository-wide while retaining the explicit `.env.example` template exception | `TestRepositoryGitignoreProtectsEnvironmentCredentials`; `git check-ignore` contract checks |
| RR-39 | A first CommandResult could reference an unknown intent or carry a different CommandID/type/ClientID, and an ambiguous result could later change command identity | HIGH | `runtime/journal/journal.go`, command-result identity regressions | Require the first result to match its retained intent, keep command identity stable across ambiguous transitions, reject malformed append/replay, and idempotently copy a recovery-injected intent into the active journal before event-derived resolution | `TestCommandResultMustMatchItsIntentIdentity`, `TestAmbiguousResultTransitionMustPreserveCommandIdentity`, raw replay tests plus existing live/reconcile in-flight resolution regressions |
| RR-48 | `FileJournal` accepted short writes as successful, and a write, sync, or post-write state-application failure could leave the process appending after the durable tail had become ambiguous | HIGH | `runtime/journal/journal.go`, `runtime/journal/file_failure_regression_test.go` | Require a full framed write; convert nil-error short writes to `io.ErrShortWrite`; poison, close, and detach the appender after every write/sync/post-write apply failure so no later frame can be appended after an uncertain tail; preserve restart recovery by truncating only an incomplete final frame and replaying a complete sync-uncertain frame | `TestFileJournalPartialWritePoisonsAppenderAndReplayTruncatesTail`, `TestFileJournalNilErrorShortWriteReturnsErrShortWriteAndPoisons`, `TestFileJournalSyncFailurePoisonsAppenderWithoutCorruptingReplay`, `TestFileJournalPostWriteApplyFailurePoisonsAppender`; journal normal/race/vet and independent review |
| RR-49 | Journal retention checked protection by scanning the entire retained record set on every append, making append and replay cumulatively quadratic even while no compaction was possible | HIGH | `runtime/journal/journal.go`, `runtime/journal/compaction_complexity_regression_test.go` | Maintain an exact incremental unprotected-record count plus deduplicated cursor dependency reference counts; run a full classification/compaction scan only after ordinary history exceeds its bounded slack, then recalibrate from the retained set | Protected-growth scan count remains zero, 900 ordinary records scan only at retention-slack boundaries, and intent/result/finding/cursor/shared-dependency transition counts match a full classifier; journal normal/race/vet |
| RR-50 | Multiple non-blocking occurrences with the same logical Finding ID but different payload metadata reused one physical journal RecordID and failed with `ErrRecordIDConflict`; partial order snapshots with multiple missing orders could therefore abort instead of recording diagnostics | HIGH | `runtime/reconcile/state.go`, `runtime/reconcile/finding_resolution_regression_test.go` | Keep `Finding.ID` as the logical diagnostic identity, but derive each non-blocking physical report ID from the logical ID plus the complete serialized payload; exact retries remain idempotent while distinct occurrences cannot alias | `TestJournalStateStoreNonBlockingFindingPayloadVersionsDoNotConflict`, partial-scope reconciliation regression, reconcile normal/race/vet |
| RR-51 | Long-lived fill retention evicted an order-alias identity even while the same fill key remained in the independently retained overlap set, allowing a conflicting duplicate to be skipped and to resolve the wrong in-flight command | HIGH | `runtime/reconcile/reconcile.go`, `runtime/reconcile/fill_identity_conflict_test.go` | Retain fill alias identities for the union of long-lived, overlap, active-pass, observed, and pending fill sets; prune only after the fill key leaves every exact deduplication horizon | `TestReconcileOverlapRetainsFillOrderIdentityAfterLongTermIndexEviction`, prior overlap/identity/capacity regressions under normal/race |
| RR-52 | A custom `AppliedFillRecorder` could return success with an empty physical record ID; the business fill was applied while the cursor silently omitted its crash-replay dependency | HIGH | `runtime/reconcile/reconcile.go`, `runtime/reconcile/fill_durability_regression_test.go` | Reject an empty ID on all three successful recording paths; after an already-applied fill, retain the exact pending payload and fail before cursor commit so durability can be retried without a second business application | `TestAppliedFillRecorderEmptyRecordIDFailsClosedAfterBusinessApplication`, pending-flush and cursor-commit failure regressions |
| RR-53 | A custom `AppliedFillLoader` could omit, add, duplicate, or return an empty cursor dependency and reconciliation would still seed partial state and advance from the cursor | HIGH | `runtime/reconcile/reconcile.go`, `runtime/reconcile/fill_durability_regression_test.go` | Validate the requested and returned dependency sets before any replay mutation: every requested ID must be non-empty and returned exactly once, with no extras; also reject empty uncommitted-replay IDs | `TestAppliedFillLoaderMustReturnExactCursorDependencies`, restart/compaction dependency regressions, normal/race/vet |
| RR-16 | A durable duplicate fill skipped business reapplication correctly but left its replayed in-flight intent unresolved | MEDIUM | `runtime/reconcile/reconcile.go`, durable duplicate regression tests | Resolve the canonical order's in-flight intent in every durable-duplicate branch without reapplying Portfolio or callbacks | `TestDurableDuplicateFillResolvesReplayedInFlightIntent`; repeated runtime normal/race/vet |
| RR-20 | `BITGET_WS_DEBUG=1` printed complete private WebSocket payloads | MEDIUM | `sdk/bitget/private_ws.go`, `sdk/bitget/private_ws_test.go` | Log only a locally classified message kind and byte count; never print remote fields or payload values | `TestPrivateWSDebugReceiveLogDoesNotExposePrivatePayload`; independent source review and normal/race package tests |
| RR-21 | Bybit order-history, realtime orders, positions, executions, and public-instrument pagination could loop forever on repeated/cyclic or endlessly unique cursors | MEDIUM | `sdk/bybit/{order_rest,private_rest,public_rest}.go`, corresponding pagination tests | Reject repeated/cyclic cursors, cap every traversal at 1000 pages, and return no partial snapshot on incomplete traversal | Repeated-cursor and finite-page-limit tests for orders, positions, executions, and instruments; normal/race repeats and package vet |
| RR-23 | Callback-queue overflow could leave the exact Aster/Bybit/Gate socket open after its read loop aborted; Bybit ping could also outlive Close | MEDIUM | `sdk/aster/{spot,perp}/ws_client.go`, `sdk/bybit/private_ws.go`, `sdk/gate/ws_client.go`, callback overflow/lifecycle tests | Close the exact failed socket on every overflow/connection-loss return without touching a replacement; bind Bybit ping lifetime to the client context | Four exact-peer-close regressions, Bybit Close/ping liveness, package normal/race/vet and independent review |
| RR-28 | The shared lifecycle dispatcher could grow without bound while paused or while a resume hook blocked | MEDIUM | `internal/wsdispatch/ws.go`, `internal/wsdispatch/dispatcher_test.go` | Enforce a fixed paused buffer bound, fail closed on overflow, and keep the buffer bounded throughout serialized resume | `TestDispatcherPausedBufferOverflowFailsClosed`, `TestDispatcherPausedBufferStaysBoundedWhileResumeHookBlocks`; normal/race repeats |
| RR-33 | Repeated commands at a fixed clock tick, including after Engine recreation, could derive the same command/intent identity and collapse or conflict with a distinct venue operation | MEDIUM | `runtime/exec/engine.go`, `runtime/exec/engine_test.go`, `runtime/exec/command_identity_internal_test.go` | Generate every command identity with a 128-bit nonce read through an Engine-private `io.Reader` initialized from `crypto/rand.Reader`; propagate `io.ReadFull` failures before journal/cache/venue side effects without exposing a public entropy override | `TestFixedClockRepeatedModifyCommandsHaveDistinctIntentIDs`, `TestFixedClockCommandsRemainDistinctAcrossEngineRecreation`, `TestCommandEntropyFailureStopsBeforeDurableOrVenueSideEffects`; runtime normal/race/vet |
| RR-37 | An explicitly exported empty canonical environment variable could be repopulated from a legacy alias, defeating an operator's deliberate disablement | MEDIUM | `internal/testenv/testenv.go`, `internal/testenv/testenv_test.go` | Treat canonical-variable presence, including an empty value, as authoritative; populate legacy aliases only when the canonical key is truly unset | `TestLoadRepoEnvLegacyAliasDoesNotOverrideExplicitEmptyCanonical`; legacy compatibility regression |
| RR-38 | Config `String`/`GoString` output and URL-validation errors could expose endpoint/proxy userinfo, query parameters, or fragments | MEDIUM | `internal/testenv/testenv.go`, `internal/testenv/testenv_test.go` | Parse and render only sanitized URL scheme/host/path, remove userinfo/query/fragment, and replace malformed credential-bearing URLs with a redacted sentinel | `TestConfigStringsRedactEndpointAndProxyURLCredentials`, credentialed validation-error regressions; testenv normal/race/vet |
| RR-40 | Concurrent operator finding resolution, reconciliation recording, and finding reads raced on `JournalStateStore` maps and could panic or lose updates; the first mutex repair then held that mutex across injectable `Store` calls and deterministically deadlocked on legal synchronous callbacks | MEDIUM | `runtime/reconcile/state.go`, `runtime/reconcile/finding_resolution_regression_test.go` | Protect only local snapshots with a dedicated mutex and perform every external `AppendReport`/`Records` call unlocked | `TestJournalStateStoreFindingOperationsAreConcurrentSafe` is old-code Race/panic; `TestJournalStateStoreFindingOperationsPermitReentrantJournalCallbacks` is first-repair timeout/deadlock; `TestJournalStateStoreFindingLifecycleSupportsOpaqueJournalStore` preserves public Store compatibility; all pass under `-race` |
| RR-41 | Missing-dependency cursor frames could append an unbounded number of replay warnings without consuming the journal conflict-index capacity | MEDIUM | `runtime/journal/journal.go`, `runtime/journal/conflict_regression_test.go` | Cap replay warnings at 1,024 and aggregate additional omissions in the final slot without rewriting the log | `TestReplayWarningsAreBoundedAndDoNotRewriteJournal`; journal normal/race/vet |
| RR-42 | A successful finding append could remain hidden when a global replay generation changed twice, including from unrelated findings, while `RecordFinding` still returned success | MEDIUM | `runtime/reconcile/state.go`, `runtime/reconcile/finding_resolution_regression_test.go` | Replace global replay invalidation with per-FindingID versions; apply unrelated snapshot entries independently and retry a contested logical finding to a stable snapshot or return an error | `TestJournalStateStoreReplayIsNotInvalidatedByUnrelatedFindingUpdates` is second-repair fail-open and now passes repeatedly under normal/race tests |
| RR-43 | Concurrent records for the same new logical FindingID could persist multiple blocking record IDs while replay and resolution remembered only one, leaving an older record permanently retention-protected | MEDIUM | `runtime/reconcile/state.go`, `runtime/reconcile/finding_resolution_regression_test.go` | Retain every open journal record ID per logical finding and emit an exact resolution tombstone for each captured ID; remove only the resolved identity so concurrent reopens survive and a partial tombstone failure keeps every unresolved identity blocking | `TestJournalStateStoreResolvesEveryConcurrentRecordForSameFinding` is second-repair retained-orphan failure; `TestJournalStateStorePartialMultiRecordResolutionRemainsFailClosed` proves one-success/one-failure recovery and retry; normal/race retention rebuild stress passes |
| RR-44 | Stable replay overwrote newer coalesced PassID/CreatedAt/operator context with the first durable blocking-finding payload | MEDIUM | `runtime/reconcile/state.go`, `runtime/reconcile/finding_resolution_regression_test.go` | When replay produces the same durable record-ID set, preserve the newer in-memory metadata while still replacing state whenever the authoritative open-record set changes | `TestJournalStateStoreReplayPreservesLatestBlockingFindingMetadata` is second-repair stale-metadata failure and passes under normal/race stress |
| RR-45 | A synchronous RecordFinding callback after a resolution tombstone was durable still deduplicated against the about-to-close in-memory finding and was then deleted, losing a legitimate reopen | MEDIUM | `runtime/reconcile/state.go`, `runtime/reconcile/finding_resolution_regression_test.go` | Mark captured findings as resolving before external appends so any overlapping/reentrant record receives a new open record ID; resolution removes only its pre-captured IDs | `TestJournalStateStoreReentrantRecordDuringResolutionReopensFinding` is second-repair lost-reopen failure; rebuilt-journal normal/race stress passes |
| RR-46 | An exact-identical reopen reused a historical blocking RecordID, so journal idempotency could erase a reentrant reopen or any reopen after the original open/resolution records compacted from retained history | MEDIUM | `runtime/reconcile/state.go`, `runtime/reconcile/finding_resolution_regression_test.go` | Derive each new blocking-open identity from a private 128-bit StateStore-instance nonce, a per-instance identity sequence, the full payload hash, and the active resolution epoch; keep per-Finding versions exclusively as concurrency/CAS barriers; unresolved retries remain idempotent while resolution/restart incarnations cannot alias old records; entropy failure returns before local/journal mutation | `TestJournalStateStoreExactReentrantRecordDuringResolutionGetsNewIdentity` and `TestJournalStateStoreExactFindingCanReopenAfterResolvedHistoryCompacts` are third-repair failures; `TestJournalStateStoreFindingIdentityEntropyFailureHasNoSideEffects`; normal/race/vet stress |
| RR-47 | Follow-up repairs to finding replay could retain unbounded version/pending state, reopen resolved physical records through stale retries, lose newer metadata during ambiguous retries, fail open on opaque stores, and scan the journal once per finding in batch resolution | MEDIUM | `runtime/reconcile/state.go`, `runtime/reconcile/reconcile.go`, `runtime/reconcile/finding_resolution_regression_test.go` | Bound pending identities and mutation history at 1,024; reclaim inactive per-ID versions; distinguish in-flight from ambiguous appends; retry the exact original physical report with stable keys while deterministically preserving the newest logical metadata; filter non-blocking reports during replay; fail closed when ambiguous state cannot be enumerated; scope resolved physical-ID tombstones to active operations; and use per-ID CAS plus one journal scan per batch replay round | Cancellation/entropy/opaque-store/replayable-store/pending-capacity/stable-retry/updated-metadata/stale-retry/non-blocking/version-reclamation tests, `TestReconcilerBatchFindingResolutionUsesConstantJournalScans`, exact reopen across FileJournal restart, and repeated normal/race/vet independent review |
| RR-54 | Snapshot-fill inference flattened and sorted every fill report once per order snapshot, producing `O(K * R log R)` recovery work and repeatedly rescanning reports added during the same loop | MEDIUM | `runtime/reconcile/reconcile.go`, `runtime/reconcile/snapshot_fill_index_regression_test.go` | Build one typed account/instrument/client/venue presence index in linear time, use a bounded number of map probes per order, and add inferred fills to that index as they are appended so original matching semantics are preserved | Scope/wildcard/typed-alias index regression and 1,000-order/1,000-fill no-duplicate scale regression; reconcile normal/race/vet |
| RR-56 | The first reentrant-reopen repair refreshed every open finding through a linear duplicate check, making one resolution quadratic in the number of already open findings and leaving concurrent metadata stale in the report | MEDIUM | `runtime/reconcile/reconcile.go`, `runtime/reconcile/finding_resolution_regression_test.go` | Build one FindingID-to-index map and merge the authoritative open set in linear time, replacing same-ID metadata and appending only genuinely new logical findings | `TestRefreshResolvedFindingsMergesLargeOpenSetWithoutDuplicates` with 4,096 open findings plus reentrant activation regression; normal/race/vet |
| RR-57 | Memory and file append paths marshaled and validated every payload immediately before `buildPayload` performed the identical locked validation | LOW | `runtime/journal/journal.go` | Remove the redundant helper and two call sites while retaining pre-write `buildPayload` validation and the independent post-write `applyRecord` validation that protects uncertain file-state transitions | Journal conflict/identity/file-failure/compaction suites, package normal/race/vet and full repository gates |

Live-discovered Lighter, Aster, and Bybit findings plus the final repo-`.env` execution-gate finding are mapped as DV-17 through DV-21 in `2026-07-13-demo-testnet-validation.md` so their real-environment and fail-closed evidence remains adjacent to the live safety contract.

## Rejected review claims

| Claim | Disposition and evidence |
| --- | --- |
| Restart must replay every retained terminal fill into a new Portfolio | **Rejected as a false positive.** The approved P4/P5 PRD requires replayed terminal applied events to rebuild idempotency indexes only and not be emitted again. The journal retention window is not a complete Portfolio snapshot; replaying it as full business history would manufacture truncated PnL state. The architect reviewer withdrew this finding after checking the governing PRD. |
| Every `Report.Partial` value must block activation | **Rejected as over-broad.** Several adapters intentionally return authoritative open-orders-only snapshots. Activation blocks on incomplete fill history, unavailable/partial open-order evidence, or blocking findings, while the explicitly tested `OPEN_ORDERS_ONLY` condition remains safe. |
| `cmd/livedemo` never reconciles | **Rejected.** `cmd/livedemo/main.go` explicitly calls `node.Resync(ctx)` before `node.Run(ctx)`, and `Run` also owns startup reconciliation. |
| Removed backtest/options goals were missing implementations | **Rejected as out of current scope.** The approved live-only program removed backtest semantics and did not make options expansion part of this remediation. |
| `RequireAccountState` and capability gating must become unconditional | **Rejected as a compatibility-breaking hardening proposal, not a demonstrated bug.** Both fail-closed surfaces exist and are explicitly enabled/bound by adapter-backed runtime configuration; legacy fallback remains opt-in and tested. |
| File journals must rewrite/compact the physical log automatically | **Rejected for this finding set.** CR-10 concerned in-memory/replay retention. The current implementation bounds retained in-memory records while preserving append-only crash recovery; no safe atomic on-disk rewrite protocol was specified. RR-48 separately ensures every uncertain write poisons the active appender, so rejecting disk rewrite does not permit writes after an ambiguous tail. |
| Bitget raw-request errors still expose URL query parameters | **Rejected after direct verification.** The shared Bitget request path already records `EscapedPath()` rather than the raw URL; the transport/status query-sentinel regression passes and no query value reaches the error or observer output. |
| The repaired Binance Spot account generation can still publish `Recovered` for an obsolete socket | **Rejected after the repaired implementation was independently re-audited.** Completion requires both the account recovery epoch and exact transport connection epoch; disconnect-before-completion, stale-reader, and Close-before-callback regressions all fail closed under `-race`. |
| `.NOTPARALLEL` must prevent two completely separate `make` processes from running acceptance simultaneously | **Rejected as an unrequested orchestration hardening proposal, not a demonstrated repository bug.** Every credentialed aggregate is serialized within its Make invocation, the documented operator contract requires venue commands to run serially, and this Ralph run executes them one at a time. A cross-process host lock would require a separate ownership/stale-lock design. |
| Historical Hyperliquid `userFills` snapshots must be discarded | **Rejected as a false positive.** A reconnect snapshot can contain real fills that were not applied before the disconnect. `FromSnapshot` is a provenance marker used by reconciliation/deduplication, not permission to lose the event. Snapshot, identity, and restart regressions cover this behavior. |
| Hyperliquid Spot runtime must require fill-derived `Portfolio.NetQty == 0` | **Rejected as semantically invalid for Spot.** Authoritative cleanup is the account balance delta relative to the pre-test baseline, bounded sellable quantity, and permitted fee/dust reserve. Historical fill replay and base-denominated fees can leave a non-zero fill-derived Portfolio value without consuming pre-existing inventory. Perp/HIP-3 still require flat positions. |
| Every non-empty Modify response ClientID must exactly equal the request ClientID | **Rejected as over-strict.** Some legitimate venue adapters return a sparse or venue-generated replacement ClientID. Modify preserves the original logical ClientID and rejects a response ID only when it is already owned by another cached order; Submit remains strict because it creates the logical identity. |
| `RequireLiveWrite` must reject hypothetical future gate variables whose names omit the current `RUN_`, `ENABLE_`, and `ALLOW_` conventions | **Rejected as prospective hardening, not a current defect.** Every repository live/read/write gate is covered by the current process-local filter and contract tests. A future non-standard gate must extend that explicit contract when introduced; inventing an unbounded name heuristic now would not prove a present unsafe path. |
| The 3 ms delay in the cursor-watermark regression is dead code | **Rejected after direct RED verification.** Removing it made `TestCursorWatermarkDoesNotExceedRequestedFillWindow` fail because the second query's real wall-clock `Until` had not yet reached the deliberately missed fill at the first boundary plus 1 ms. The delay was restored with an explanatory comment. |
| Every 1-second channel timeout in finding concurrency regressions should be widened | **Rejected as an unproven flake claim.** These are deliberate deadlock/barrier guards, not production timeouts; the focused normal/race suites and repeated independent stress all pass. A common wider deadline may be considered if CI supplies actual flake evidence. |

## Current final evidence

- Public API: no existing exported method was removed. New contracts/options are additive; `WithInFlightResolver` has a source-compatible parameter relaxation so every prior resolver still compiles, while optional identity capabilities are detected dynamically. The custom `journal.Store` compatibility regression discovered during final review was repaired.
- `gofmt` on every changed and untracked Go file: **PASS**.
- `go test ./... -count=1`: **PASS**, exit code 0 across all repository packages with every live gate explicitly disabled.
- `go test -race ./... -count=1`: **PASS**, exit code 0 across all repository packages with every live gate explicitly disabled.
- `go vet ./...`: **PASS**, exit code 0.
- `staticcheck ./...`: **N/A**, binary not installed.
- `git diff --check`: **PASS**.
- Earlier independent runtime/architect rounds: **PASS after repair** for the CR/RR findings above.
- Final post-live-discovery runtime, journal, SDK, and adapter/live-safety reviews: **PASS after repair**. The final independent runtime/journal reviewer found no remaining actionable blocker and repeated key regressions under normal/race modes.
- Changed-files-only ai-slop review: **PASS** after the final accepted finding. Its duplicate Journal prevalidation cleanup was accepted; its proposed cursor-test sleep deletion was independently disproved by a RED regression and reverted.
- Fresh serialized Demo/Testnet evidence: Nado complete aggregate **PASS**; Hyperliquid standard Spot/Perp read, adapter, and runtime rows **PASS**, with HIP-3 stopped before writes because its configured live book was empty. See the separate validation record for every venue and historical-versus-fresh evidence.
- Final live-safety audit found that repo `.env` activation flags could be imported into an ordinary test process. `TestLoadRepoEnvDoesNotImportExecutionGates` reproduced the issue before repair; targeted normal/race tests, vet, and the final full repository gates pass after making all execution/unsafe-mode gates process-local only.

## Remaining blockers and limitations

- Binance, OKX, Bybit, and Bitget fresh reference probes are blocked before usable connectivity on both configured proxy and direct paths. Their write gates were not enabled after those prerequisite failures; final code paths remain covered by offline normal/race regressions.
- Gate fresh reference/read/Spot-adapter rows passed, but Spot runtime then hit a transport timeout; later write rows were stopped. A historical complete aggregate exists, but the sanitized failure does not prove whether submit/cleanup/final snapshot completed, so current open-order/base-balance state is unproven rather than assumed clean.
- Aster fresh reference/read/Spot adapter+runtime/Perp adapter rows passed, while Perp runtime failed twice on an unresolved cleanup/account-state condition. No speculative compensating order or account-wide cleanup was issued.
- Hyperliquid standard Spot/Perp fresh adapter and runtime rows passed. HIP-3 read failed twice on an empty real-time book, so both HIP-3 write rows were deliberately withheld.
- Early pre-fix Aster Spot and Hyperliquid Spot runs left bounded test-asset residuals. Exact safe cleanup quantities are documented in the live validation record, but cleanup is intentionally withheld unless baseline ownership, current free balance, minimum notional, no unrelated orders, and exact post-submit reconciliation can all be proven.
- `staticcheck` is unavailable in this environment.
- File-journal physical disk usage remains append-only; in-memory replay state is bounded, but automatic atomic disk compaction remains a separate design task rather than an unverified rewrite in this remediation.
